{
  "name": "blockchain-certificates-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "format": "prettier --write \"**/*.{ts,js,json,md}\"",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run --reporter=verbose",
    "migrate": "prisma db push",
    "seed": "tsx prisma/seed.ts",
    "prisma:generate": "prisma generate",
    "ci:local": "npm run format && npm run lint && npm run build && npm run prisma:generate && npm run migrate && npm run seed && npm run test && npm run start"
  },
  "dependencies": {
    "@fastify/jwt": "^8.0.1",
    "@fastify/multipart": "^8.2.0",
    "@fastify/swagger": "^9.4.0",
    "@fastify/swagger-ui": "^1.10.2",
    "@prisma/client": "^5.18.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "pino-pretty": "^11.3.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20.14.12",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "prisma": "^5.18.0",
    "tslib": "^2.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
